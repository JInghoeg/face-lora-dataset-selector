# Face LoRA Dataset Selector v0.3.0

发布日期：2026-09-25

这是经过撤回后的修复线重新完成自动验证和真实人工 QA 后发布的正式 v0.3.0。

v0.3.0 把原来的“质量筛选 + 去重 + 人工确认”扩展成了一套完整的 LoRA 数据集工作流，同时仍然坚持：**自动判断只提供建议，最终决定权在人。**

## 推荐下载

**Windows x64 Portable**

下载：

`Face-LoRA-Dataset-Selector-Windows-x64-Portable.zip`

完整解压后运行：

`Face LoRA Dataset Selector.exe`

不需要另外安装 Python，也不需要 CUDA。

## v0.3.0 主要变化

### 完整的数据集复核流程

现在推荐顺序是：

```
初始分析 / 推荐
-> 重复组复核
-> 组合图拆分
-> 自动裁剪
-> 可选：整理源文件
-> 最终训练导出
```

- 新增 Dataset / View 筛选、排序、分页、保存视图和稳定 sample ID。
- 重复组复核支持持久化的人类决定和整组处理。
- F5 变成增量刷新：未变化图片复用缓存，只对新增/修改内容做重分析。

### 组合图拆分

- 可把独立多视角 / 多人物组合图拆成多个真实数据集文件。
- 重叠多人构图可输出一个保守群组裁剪。
- 每个输出都可单独勾选：勾选 -> 推荐；取消勾选 -> 淘汰。
- 接受后原组合图会移入 `_CompositeSplit_Originals/` 隔离目录。
- 隔离原图不会再进入重复组、自动裁剪、整理源文件、AI Bundle 或最终训练导出。

### 自动裁剪

- 使用 ISNetIS 做保守主体范围建议，不使用固定宽高比。
- 裁剪建议在最终训练导出前一直只是元数据，不改写源图片像素。
- 支持拖动 / 四边四角缩放 ROI、重置、接受、保留原图、恢复待定。
- 新的 Fluent Filmstrip 复核界面支持真实缩略图、Light/Dark、竖向滚动和可拖拽展开。
- 默认候选区只显示一整行；向上拖动后才显示更多行。
- ISNetIS 模型第一次使用时按需下载并做 SHA-256 校验，不直接塞进基础 Portable ZIP。

### 整理源文件

- 可选地把当前活动图片移动到 推荐 / 备选 / 淘汰 目录。
- 执行前先生成计划并要求明确确认。
- 保留原相对目录，检查冲突，并在移动失败时执行回滚。
- **这个功能会真实移动文件。第一次使用建议先在数据集副本上测试。**
- 不会把自动裁剪像素写回源图片。

### AI Review Bundle

- 可导出整个数据集或当前 View 给 AI 辅助检查。
- 稳定 sample ID 让建议可以准确回到本地图片。
- 导入的 `review_patch.json` 只进入独立“AI 建议层”，不会自动覆盖人工决定。

### 中英文实时切换

- 新增永久 Qt i18n 基础设施。
- 默认中文 `zh_CN`，首个翻译语言为 `en_US`。
- 可以在运行中实时切换，不需要重启。
- 语言选择会持久化。

### 字幕 / 水印清理

- 保留 PP-OCRv5 文字检测、手工增删框、MI-GAN、TELEA、Navier-Stokes 和批处理。
- 检测 + 修复继续作为一个独立功能模块维护。
- 修复输出仍然与源文件分离。

## 验证

本次正式 v0.3.0 发布前完成：

- Python 3.9 / 3.12 生产 UI 与功能回归；
- 架构边界 CI；
- Windows Portable 构建；
- 打包后 EXE self-test；
- Auto Crop Light/Dark production render 人工验收；
- 集中 Stage 4 人工 QA：PASS；
- Source Organizer 在数据集副本上进行人类验证。

## 注意

- Portable 仍未做商业代码签名，Windows SmartScreen 可能在第一次启动时提示。
- MI-GAN 和 Auto Crop ISNetIS 属于按需模型，第一次实际使用对应功能时可能需要联网下载。
- “整理源文件”和接受“组合图拆分”可能移动文件位置；训练导出、文字修复和自动裁剪不会覆盖源图片像素。
