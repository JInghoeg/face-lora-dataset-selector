# Face LoRA Dataset Selector

一个用来整理 **人物 / 人像训练数据集** 的 Windows 桌面工具。

从网上收图、视频抽帧、写真图集、自己拍摄或生成素材，攒到几十、几百张以后，真正开始训练之前通常还得做一遍很耗时间的整理：去掉明显差图和重复图，看角度和景别有没有偏科，拆拼图，清字幕和水印，重新裁掉多余画面，最后再决定哪些图真正进入训练集。

Face LoRA Dataset Selector 就是用来做这一步的。

它不负责训练模型，也不是一个“一键自动选图器”。它把人物数据集的分析、筛选、复核和整理集中到一个本地工作流里，让自动分析先帮你缩小范围，最后怎么取舍仍然由你自己决定。

<!-- README hero screenshot: docs/assets/readme/hero.png -->

## 它和普通的图片筛选有什么不同？

### 不只看清晰度，也不只看脸

人物训练集里，“这张图清不清楚”只是很小的一部分。

这个工具会同时整理人脸质量、整图质量、头部角度、身体可见范围、景别和近重复关系。你可以用它准备人物身份、人脸特征、身材 / 体型、服装造型、角色外观，以及以人物为主体的摄影或视觉风格数据集。

有些训练目标更看重脸，有些更看重全身、服装或身体比例。工具提供这些信息，但不会预设所有数据集都应该按同一种标准筛。

### 看的是整套数据，不只是单张图片

单张图质量都不错，并不代表放在一起就是一套好的训练集。

常见的问题反而是：

- 一半都是同一组连拍；
- 正脸很多，侧脸几乎没有；
- 大头照很多，全身和半身严重不足；
- 想训练服装或身材，但人物身体经常被裁掉；
- 某一种构图、姿态或场景反复出现。

所以这里除了单图质量，也会看近重复、头部角度和景别分布，帮助你发现整套数据里的偏科。

### 自动分析，但不替你做最终决定

不同 LoRA、不同模型、不同训练目标，对“好数据”的要求并不一样。

自动结果可以帮你优先找到值得看的图片、近重复组和明显问题，但推荐 / 备选 / 淘汰都可以人工修改。v0.3 还把重复组、组合图和裁剪单独做成复核流程，而不是在后台自动处理完就算结束。

对于已经有自己数据集标准的用户，这些分析更像是节省人工时间的工具，而不是一套必须照着执行的规则。

### 不只是“筛掉”，也尽量处理还能用的素材

有价值的图片不一定一开始就是标准训练图。

项目目前已经覆盖或正在加入：

- 近重复复核；
- 多人物 / 多视角组合图拆分；
- 自动裁剪和手动 ROI；
- 字幕、水印和其他文字区域清理；
- 最终训练集导出。

也就是说，流程不只是判断“留还是删”，还可以把一部分原本需要额外软件处理的素材继续整理到可用状态。

### 尽量不碰原图

分析、筛选状态、自动裁剪建议、训练导出和文字修复都不会直接覆盖源图片。

确实需要移动文件的操作会单独提示。v0.3 的组合图拆分和 Source Organizer 也把这类行为和普通分析明确分开。

### 本地运行，普通 Windows 机器也能用

稳定版提供 Windows x64 Portable，解压后直接运行，不需要先搭 Python 环境，也不要求 NVIDIA 显卡或 CUDA。

图片分析和人工复核都在本地完成；少数可选模型第一次使用时才会按需下载。

---

## 适合哪些数据集？

不管你是第一次准备自己的 LoRA，还是已经长期在做 AI 生图、LoRA 训练或模型微调，只要素材主要是人物 / 人像图片，这套流程都可以用来做训练前整理。

例如：

- 真人 / 人物身份 LoRA；
- 更强调脸部特征的数据集；
- 身材、体型、身体比例相关训练；
- 服装、造型、角色外观 LoRA；
- 以人物为主体的摄影、视觉或其他风格训练；
- 其他需要人物图片筛选、去重、裁剪、拆分和人工复核的数据集。

如果你的数据完全不依赖人物信息，例如纯场景、纯产品或其他非人物数据集，当前的人脸、姿态和景别指标就不一定合适。

---

## 稳定版和当前开发版

**当前稳定版本：v0.2.0**

**[下载 v0.2.0](https://github.com/JInghoeg/face-lora-dataset-selector/releases/tag/v0.2.0)**

如果只是想正常使用，建议下载 v0.2.0 Portable。

当前仓库正在开发 **v0.3**。它在 v0.2.0 的筛选和文字清理基础上加入了更完整的人工复核流程，但目前还没有作为稳定版本发布。

| 功能 | v0.2.0 | 当前 v0.3 开发版 |
| --- | :---: | :---: |
| 人脸 / 图像质量分析 | ✓ | ✓ |
| 角度、景别辅助分析 | ✓ | ✓ |
| 推荐 / 备选 / 淘汰筛选 | ✓ | ✓ |
| 近重复检测 | ✓ | ✓ |
| 字幕 / 水印 / 文字清理 | ✓ | ✓ |
| 独立重复组复核 | — | ✓ |
| 组合图拆分 | — | ✓ |
| 自动裁剪 + 手动 ROI | — | ✓ |
| 源文件整理 | — | ✓ |
| 中英文实时切换 | — | ✓ |

版本变化见 [CHANGELOG.md](CHANGELOG.md)。

---

## 主要功能

### 数据集分析与筛选

程序会整理人脸质量、整图质量、头部朝向、身体可见范围和近重复关系，并把结果放到同一个浏览和筛选界面里。

你可以继续按自己的训练目标筛选、排序和修改结果，不需要被一个总分牵着走。

### 近重复处理

连拍、视频抽帧和同一套写真里经常会出现大量几乎一样的图片。

程序会先找出近重复素材，再结合质量信息帮助比较，减少在同一组图片里来回翻看的时间。

### 组合图拆分 · v0.3

多视角、多人物或拼在一起的素材，可以先生成拆分候选，再由你决定哪些区域真正值得保留。

不会因为检测到多个人，就自动把所有区域都塞进训练集。

### 自动裁剪 · v0.3

对需要收紧构图的图片给出裁剪建议，并允许手动调整 ROI。

裁剪建议不会直接改写源图；最终导出训练集时，接受的裁剪才会写入输出图片。

### 字幕 / 水印 / 文字清理

Text Cleanup 会检测图片里的文字区域，也可以手动增删检测框。

支持 TELEA、Navier-Stokes 和可选的 MI-GAN AI 修复。可以先看预览，再批量输出到新目录。

### 整理与导出

完成筛选后，可以把最终保留的素材导出为训练集。

v0.3 还增加了独立的源文件整理流程；真正移动文件之前会先给出计划和提示。

<!-- Feature screenshots:
docs/assets/readme/duplicate-review.png
docs/assets/readme/auto-crop.png
docs/assets/readme/text-cleanup.png
-->

---

## 一个典型的整理流程

v0.3 的完整流程大致是：

```text
原始人物素材
      ↓
初始分析与筛选
      ↓
近重复复核
      ↓
组合图拆分
      ↓
自动裁剪 / 人工调整
      ↓
源文件整理（可选）
      ↓
导出训练集
```

Text Cleanup 是独立功能，需要处理字幕、水印或图片文字时再使用。

v0.2.0 已经可以完成核心分析、筛选、近重复检测、文字清理和导出；组合图拆分、自动裁剪和源文件整理属于 v0.3 新增流程。

---

## 为什么不直接按“质量分数”选前 N 张？

因为人物训练数据里，单张图片质量高，不代表整套数据就合适。

只看总分，很容易最后留下来的几乎全是正脸大头照，或者同一套连拍占了一大半。对于身材、服装、角色外观等训练目标，这种偏科同样会影响数据覆盖。

这里会把几个问题分开看：

```text
图片是否基本可用
→ 人脸与整图质量
→ 近重复
→ 头部角度
→ 景别 / 身体可见范围
→ 人工确认
```

这些分析只是帮你更快看清数据里的问题，最后怎么取舍还是按自己的训练目标来。

---

## 下载 v0.2.0

Windows 用户可以直接使用 Portable：

**[Face LoRA Dataset Selector v0.2.0 Release](https://github.com/JInghoeg/face-lora-dataset-selector/releases/tag/v0.2.0)**

下载：

```text
Face-LoRA-Dataset-Selector-Windows-x64-Portable.zip
```

完整解压后运行：

```text
Face LoRA Dataset Selector.exe
```

Portable 已包含 Python 和运行依赖，不需要另外安装 Python，也不需要 CUDA。

<details>
<summary><strong>从源码运行</strong></summary>

环境：

- Windows 10 / 11 x64
- Python 3.9–3.12
- 推荐 Python 3.12 x64

下载或克隆仓库后运行：

```text
安装.bat
```

安装完成后：

```text
启动.bat
```

也可以手动创建虚拟环境：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe app.py
```

暂不建议 Python 3.13+。

</details>

---

## 快速开始

### 整理人物训练数据集

1. 选择图片目录并完成初始分析。
2. 先处理明显差图和近重复素材。
3. 看一下不同角度、景别和身体可见范围是否过于集中。
4. 按自己的训练目标继续人工筛选。
5. 最后导出真正要用于训练的图片。

如果使用 v0.3 开发版，还可以在同一流程里继续处理组合图和自动裁剪。

人物一致性、表情、服装、姿态、身材、画面风格，以及你希望最终模型学到什么，仍然需要自己判断。

### 清理字幕 / 水印

选择输入目录后，程序会检测文字区域。可以修改检测框、选择修复方式、先看预览，再批量输出到新目录。

如果文字正好覆盖眼睛、五官或其他重要训练特征，很多时候直接不用这张图会比强行修复更合适。

---

## 分析用了什么？

不同问题由不同方法处理：

| 用途 | 方法 |
| --- | --- |
| 人脸检测 | YuNet |
| 人脸质量 | eDifFIQA-T |
| 整图质量 | BRISQUE |
| 头部姿态 / Yaw | 3DDFA-V2 |
| 身体关键点 / 景别辅助 | MediaPipe Pose |
| 近重复 | pHash + 质量排序 |
| 文字区域检测 | PP-OCRv5 |

这些指标负责提供信息和候选排序，不会单独决定一张图片最终该不该进入训练集。

相关项目与资料：

- [eDifFIQA](https://github.com/LSIbabnikz/eDifFIQA)
- [BRISQUE / OpenCV Quality](https://docs.opencv.org/4.x/d8/d99/classcv_1_1quality_1_1QualityBRISQUE.html)
- [3DDFA-V2](https://github.com/cleardusk/3DDFA_V2)
- [YuNet / OpenCV Zoo](https://github.com/opencv/opencv_zoo/tree/main/models/face_detection_yunet)
- [MediaPipe Pose Landmarker](https://developers.google.com/mediapipe/solutions/vision/pose_landmarker)

---

## 原图和文件安全

大多数工作流不会覆盖原始图片。

分析结果、筛选状态、自动裁剪建议、训练导出和 Text Cleanup 修复都会保存在缓存或新的输出位置。

v0.3 中有两类操作会改变文件位置，因此会明确提示：

- 接受组合图拆分后，原组合图会移动到数据集内的隔离目录；
- 使用 Source Organizer 时，会在确认计划后移动活动图片。

第一次使用这类文件整理功能时，建议先用数据集副本熟悉流程。

---

## 模型下载与缓存

大部分基础功能所需的模型会随程序提供。

少数可选功能会在第一次使用时按需下载模型，例如 MI-GAN。下载完成后会校验并保存到 FaceLoRA 的模型缓存中，后续可以继续复用。

可选模型下载失败不会影响与它无关的其他功能。

---

## 文档

完整的使用教程会逐步整理到 GitHub Wiki。

项目架构和开发文档：

- [Architecture](docs/ARCHITECTURE.md)
- [Module Guide](docs/MODULE_GUIDE.md)
- [Development Workflow](docs/DEVELOPMENT_WORKFLOW.md)
- [v0.3 Roadmap](docs/ROADMAP_v0.3.md)

第三方模型、组件和许可证信息：

[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)

---

## License

本项目代码采用 **GPL-3.0-only**。

可以使用、修改和分发本项目；如果分发基于本项目的衍生版本，需要继续遵守 GPLv3 的相关要求。

第三方模型、库和资源遵循各自的许可证。
